spring:
  application:
    name: spring-tp

  # Profil actif par défaut
  profiles:
    active: dev

  # JPA config commune
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true



server:
  port: 8081